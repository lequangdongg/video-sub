{
  "name": "autosub-ui",
  "version": "1.0.0",
  "private": true,
  "description": "Tailwind build for the Auto Sub web UI",
  "scripts": {
    "build:css": "tailwindcss -i webapp/styles/input.css -o webapp/static/app.css --minify",
    "watch:css": "tailwindcss -i webapp/styles/input.css -o webapp/static/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
